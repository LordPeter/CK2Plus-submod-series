name = "VIET+"  # Name to use as a dependency if making a sub-mod
path = "mod/VIET+"
user_dir = "VIET+"  # Ensure we get our own versions of the gfx/map caches and savegames	# Not sure if this is needed, leave it for now
dependencies = { "CK2Plus" }	# Should overwrite the CK2+ files necessary *fingers crossed*